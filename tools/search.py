async def web_search(query: str, max_results: int = 5) -> str:
    """Search the web via DuckDuckGo. Full implementation in Stage 5."""
    return "web_search not available until Stage 5 (requires duckduckgo-search)."


async def web_search_raw(query: str, max_results: int = 5) -> list[dict]:
    """Return raw search results. Full implementation in Stage 5."""
    return []
